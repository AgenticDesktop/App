项目通过 `<UseWinUI>true</UseWinUI>` 和 `Microsoft.Windows.SDK.BuildTools.WinApp` 启用 `dotnet run` 直接运行打包的 WinUI 应用；调试时自动通过 winapp CLI 注册 AUMID 并启动；发布配置位于 `Properties/PublishProfiles/win-{x86,x64,arm64}.pubxml`，Release 构建开启 ReadyToRun 和 Trimming。